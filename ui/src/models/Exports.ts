export enum Status {
    Loading = 'loading',
    Error = 'error',
    Success = 'success',
    Idle = 'idle'
}

export enum ConversionMode {
    Decode = 'decode',
    Encode = 'encode',
    None = 'none'
}

export enum ExportType {
    Base64 = 'base64',
    PNG = 'PNG',
    XML = 'XML'
}