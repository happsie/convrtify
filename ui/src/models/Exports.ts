export enum Status {
    Loading = 'loading',
    Error = 'error',
    Success = 'success',
    Idle = 'idle'
}

export enum ConversionMode {
    From = 'from',
    To = 'to'
}

export enum ExportType {
    Base64 = 'base64',
    PNG = 'PNG',
    XML = 'XML'
}